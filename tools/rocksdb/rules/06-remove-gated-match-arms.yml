id: remove-rocksdb-gated-match-arms
language: rust
rule:
  any:
    - pattern:
        context: |
          match v {
              #[cfg(feature = "rocksdb")]
              $PAT => $VAL,
          }
        selector: match_arm
    - pattern:
        context: |
          match v {
              #[cfg(feature = "rocksdb")]
              $PAT => $VAL
          }
        selector: match_arm
fix: ""
