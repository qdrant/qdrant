android.useAndroidX=true

# Maven publish coordinates. VERSION_NAME mirrors `lib/edge/VERSION` (the native
# SDK's single source of truth); keep them in sync — CI verifies it.
GROUP=tech.qdrant
VERSION_NAME=0.7.2
