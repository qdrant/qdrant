layout(set = 0, binding = 0) buffer ScoresData {
    float data[];
} scores;

void main() {
    uint idx = SUBGROUP_ID;
    set_vector_storage_target(0);
    float score = similarity(idx);
    if (subgroupElect()) {
        scores.data[idx] = score;
    }
}
