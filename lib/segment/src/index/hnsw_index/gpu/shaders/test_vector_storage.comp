#version 450

#define VECTOR_STORAGE_LAYOUT_SET 0
#include "vector_storage.comp"

layout(set = 1, binding = 0) buffer ScoresData {
    float data[];
} scores;

void main() {
    scores.data[gl_GlobalInvocationID.x] = 42.0;// similarity(0, gl_GlobalInvocationID.x);
}
