log_level: INFO

service:
  host: 0.0.0.0
  http_port: 6333
  # Uncomment to enable gRPC:
  #grpc_port: 6334

# Enable or disable per-collection metrics (default false)
metrics:
  record_per_collection: false
